<?xml version="1.0" encoding="utf-8"?>
<!-- Hourly refresh so "today" rolls over; the app also refreshes widgets whenever food is logged. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_nutrition_description"
    android:initialLayout="@layout/widget_nutrition"
    android:minWidth="250dp"
    android:minHeight="150dp"
    android:previewLayout="@layout/widget_nutrition"
    android:resizeMode="horizontal|vertical"
    android:targetCellWidth="4"
    android:targetCellHeight="2"
    android:updatePeriodMillis="3600000"
    android:widgetCategory="home_screen" />
