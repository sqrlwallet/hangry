<?xml version="1.0" encoding="utf-8"?>
<!-- 30 min refresh so "today" rolls over after midnight; dose changes push updates directly. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_supplements_description"
    android:initialLayout="@layout/widget_supplements"
    android:minWidth="110dp"
    android:minHeight="110dp"
    android:previewLayout="@layout/widget_supplements"
    android:resizeMode="horizontal|vertical"
    android:targetCellWidth="2"
    android:targetCellHeight="2"
    android:updatePeriodMillis="1800000"
    android:widgetCategory="home_screen" />
